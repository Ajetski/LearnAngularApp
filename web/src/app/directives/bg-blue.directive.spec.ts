import { BgBlueDirective } from './bg-blue.directive';

describe('BgBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new BgBlueDirective();
    expect(directive).toBeTruthy();
  });
});
